## Packages
date-fns | Date formatting and manipulation for calendar/time slots
lucide-react | Icons for UI elements (already in base but explicitly required)
framer-motion | Smooth transitions between booking steps
react-day-picker | Calendar component for date selection
clsx | Utility for conditional class names
tailwind-merge | Utility for merging tailwind classes

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
